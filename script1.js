function register(registerform) {
    if (registerform.password.value==registerform.password2.value)
        window.location.replace("https://www.youtube.com/watch?v=dQw4w9WgXcQ");
        else
            prompt("hasła się nie zgadzają");
}
